// Function with Return type
func addNumbers(num1: Int, num2: Int) -> Int {
    let sum = num1 + num2
    return sum
}
var addition = addNumbers(num1: 10, num2: 30)
print("Addition : \(addition)")

// ---------------------------------------------------

// Find squares (new example)
func findSquare(num: Int) -> Int {
    let result = num * num
    return result
}
var square = findSquare(num: 10)
print("Square : \(square)")

